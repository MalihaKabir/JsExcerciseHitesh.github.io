const numbers = [ 'One', 'Two', 'Three', 'Something', 'Five']

console.log(numbers.slice(2, 4)); // slice(starting position, 4 means - it'll give every element till position 4. Check this out by your own by using different integers.)
